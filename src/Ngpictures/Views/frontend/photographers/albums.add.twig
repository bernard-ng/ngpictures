{% extends 'layout/posts/default.twig' %}
{% import 'includes/form.twig' as form %}

{% block content %}
    <section class="section container row">
        <section class="section nexted col l9 m12 s12">
            <form action="" method="POST" class="card-panel grey dark-4">

                {{ form.input('title', 'Title', 'text', errors.get('title'), post.get('title')) }}
                {{ form.textarea('description', 'Description', errors.get('description'), post.get('description')) }}
                <div class="row col s12" style="margin-top: 20px; margin-bottom: 30px;">
                    <input type="checkbox" class="filled-in cyan darken-2" id="remember" name="remember" value='1' />
                    <label for="remember">Album Public</label>
                </div>

                <div class="submit-button">
                    <button type="submit" class="btn btn-flat waves-effect">Ajouter</i>
                </button>
                </div>
            </form>
        </section>
        {% include 'includes/menu-aside.twig' %}
    </section>
{% endblock %}
